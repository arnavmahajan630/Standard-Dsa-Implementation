### Climbing Stairs

* How is this DP ?  (essentially recursion with optimization)
=> Count total ways, mutliple ways of doing something ? min,max way
* Try all Possible ways ? best way, counting

## 1D DP Problems

* Represent the problem in index
* define the recursive func for number of ways the core of the problem 
* Always return the DP value first
* Then calculate the dp value using the recursive function